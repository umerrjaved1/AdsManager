# TeraFort Ad Manager (Android)

Android library module located at `adsManager/`.

## Build
- JDK 11
- Android Gradle Plugin via versions catalog
- Gradle wrapper: `./gradlew assembleRelease`

## Publish (local)
The module contains a `publishing` block for Maven. Adjust `groupId`, `artifactId`, `version` as needed.
