<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Android 15+ variant. Same rule as the base style: it must keep the SDK's own ad theme as its
      parent, or AdActivity loses its translucency and its no-title window. Add API 35 specific
      overrides here as items, never by dropping the parent.
    -->
    <style name="AdmobTheme" parent="@style/Theme.GoogleMobileAds" />
</resources>
